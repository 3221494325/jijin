# 2026-08-22 工作日志

- 初始化 FundOS 项目结构与 WorkBuddy 记忆体系。
- 现状：已有完善 `AGENTS.md`（项目级规范）、`ARCHITECTURE.md`（架构/技能库/数据流）、`.codex/skills/fundos/` 22 技能库、多份数据脚本与 JSON 数据。
- 本次仅建立记忆骨架，未改动脚本与数据。

## 19:42 全量文件盘点
- 用户要求：先全量盘点项目所有文件；并明确长期职责 = 基金持仓管理（诊断/盯盘/写操作思路）。
- 产出 `项目文件盘点.md`：共 325 个文件（剔除本次临时脚本），19.16 MB。
  - 技能库 `.codex` 266 个/19.2 MB（FundOS 人格+22 蒸馏技能，占绝对大头）
  - 数据 10、脚本 9、文档 9、归档 30、备份 1。
- 注：`.firecrawl/` 为第三方爬虫缓存（技能摄取残留），非核心业务文件。

## 19:44 总裁批示 · 周六盘后诊断（选项1：用现有快照）
- 依据：权威快照 `portfolio_snapshot.json`(2026-08-08) + 最近扫描 `market_sector_scan.md`(2026-08-21 周五收盘)。周六休市，不跑实时。
- 结论：组合 -2.50%(-¥94.99)，距回本仅2.5%。主线已轮动（医药→有色/黄金/锂矿/电子），持仓板块落后主线4.4pp。
- 三处结构问题：① 全球成长-19.15%吃掉79%亏损（主要矛盾，停定投+反弹减仓/-25%硬止损）；② 科技48.4%超配(阈40%)；③ C类占59.3%。
- 交付：`总裁批示_2026-08-22.md`；并往 `trade_journal.json` 留痕4条(全组合/全球成长/信息产业/科技组)，现共37条。
- 发现数据隐患：`fundos_core.py`(硬编码旧代码018354等且不读snapshot)、`trade_journal.py`(FUNDS旧代码+交互式) 均已过时，建议改为读取 `portfolio_snapshot.json`。待用户确认后修复。

## 19:51 修复脚本（用户"动手"确认）
- `fundos_core.py` v2.2：改为从 `portfolio_snapshot.json` 读权威持仓（8只/2026-08-08），代码表已纠错(018735/012922/017641/019172/019764/011608/022365/019018)；净值取不到时市值以快照为准不再归零。实测：合计 ¥3803/¥3708/-¥95/-2.50%，与快照一致，且成功拉到实时指数/净值。
- `trade_journal.py` v1.1：FUNDS 代码纠错；新增非交互式 `add --code --action --reason --expect`（便于自动化留痕），交互模式保留。实测非交互留痕跑通(#38)后清理，日志维持37条。
- 经验沉淀：Git Bash 下 `/tmp` 路径与托管 Python 解析不一致，临时脚本须用绝对 `D:/...` 路径；本次修复可固化为技能（FundOS 脚本数据源校正）。

## 19:53 设定盯盘自动化（用户"可以设定"确认）
- 创建 recurring 自动化 `FundOS 周一开盘盯盘诊断`（id: automation-1787399631045），status=ACTIVE。
- 计划：每周一 09:35 开盘后自动跑 market_scan.py + fundos_core.py + (必要时 deep_news.py)，出总裁批示并写入 `总裁批示_YYYY-MM-DD.md`，用 trade_journal.py add 留痕。
- validFrom=2026-08-24T09:35（首次下周一），rrule=FREQ=WEEKLY;BYDAY=MO，cwds=D:\基金项目。
- 说明：基金当日净值约20:00才公布，开盘后跑诊断用的是 T-1 净值快照 + 实时板块指数；完整盘后净值诊断可在收盘后另补。
