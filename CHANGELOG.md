# FundOS 更新日志（CHANGELOG）

> 倒序记录。每次大版本列【新增/修复/验证】。配套总档案见 [PROJECT_HANDOVER.md](PROJECT_HANDOVER.md)。

---

## 2026-09-08 · 快照可靠性与电脑同步

- 手机快照增加来源、北京时间、时区和状态元数据。
- 新增 sync_mobile_snapshot.py：数据变化时才提交并推送电脑快照。
- 明确当前公开 Pages 会发布持仓金额等财务字段，并增加快照结构测试。

## 2026-09-07 · 投资驾驶舱第一版

- 保留 Python 接口和现有模块，改为浅色卡片布局、导航、四项主指标与辅助风险指标；金额保留两位小数。
- 新增优先关注、数据日期与滞后汇总、持仓分类分布、持仓排序；完整指南可展开，未编造风险评级或组合历史曲线。
- 请求失败显式提示旧数据可能性，AI 配置存在但内容缺失时降级显示。
- 验证：原有 45 项测试通过；新增 `node tests/test_dashboard.cjs` 离线渲染契约检查通过。尚未完成真实浏览器视觉验收。
- 现有 8899 服务启动时缓存 HTML，需重启该服务才能显示新模板；本轮额外预览进程启动被工具策略阻止。

## 2026-08-30/31 · 行业前瞻固化进指南引擎

- `fundos_advisor.build_industry_outlook()`：主线跳变识别（排名跳变≥30名或新进）、连续在榜确认（≥2天）、持仓板块传导预警（相关板块跌超1%且影响仓位≥5% → 观察级"禁接飞刀"指令）、强弱差（主线前5均涨 - 持仓板块均涨，>2% 提示风格偏移）。全部为动量事实+应对预案，禁止预测性表述（合规边界）。
- dashboard 接线：今日板块榜（5分钟缓存）+ 近5日板块快照 → 指南卡新增"行业前瞻"行；观察级指令自动并入总裁批示。
- 实测：传媒娱乐新进第1（首日不追）、家具行业连续2天确认、强弱差 +1.76% 正确渲染。
- 单元测试 44 项（新增行业前瞻跳变/连续确认 2 用例）。

---

## 2026-08-30 · 面板进化为操作指南 + AI 研判 + 免费大模型接入

**新增**
- `fundos_advisor.py` 操作指南规则引擎：环境判定（偏暖/中性/防守）→ 分级指令（指令/警告/观察，每条含依据/动作/边界/规则出处）→ 单基金建议（触发线/换A避费/距离字段）→ 今日禁止清单。新闻情绪只进观察层，永不单独触发指令。
- `fundos_llm.py` 大模型接入层：OpenAI 兼容协议，默认智谱 GLM（glm-4-flash-250414 永久免费档）；结构指纹缓存（指令/建议/禁止清单变化才需重生成）；未配置密钥时全链路降级纯规则模式。
- `setup_llm.py` 免费大模型一键接入向导：内置智谱/硅基流动/魔搭三家免费供应商预设，交互式或命令行（`--provider bigmodel --key XXX`），自动验证连通并持久化到 `data/llm/config.json`（已 gitignore）。
- `dashboard.html` 新增两个版块：「今日操作指南」头版专栏、「AI 总裁研判」社论版块（按钮触发 `/api/llm/refresh`，未配置时显示接入教程）。

**修复**
- 情绪分类方向丢失 bug：`classify_sentiment_v2` 返回绝对值分导致"略偏空2分"被判利好 → 改带符号分值；新增 ±2 分定性阈值（弱信号一律中性，"宁可少说不乱说"）；词表扩充（被罚/罚款/警示函/减持/立案调查/上涨/收涨/涨/跌等）+ 数字涨跌幅正则（"涨8.00%"类无关键词新闻可识别）。11 个真实误判用例全部修正。
- AI 缓存指纹空转：时间戳/小数权重/单条新闻导致指纹每刷必变 → 改结构指纹（剥离括号内数字等），并有稳定性单测。
- AI 版块空白问题：市场结构变化后显示旧研判 + "⚠️ 建议重新生成"标记，而非空白。

**当日新闻归档（解决"AI 看不到全天新闻"）**
- `data/news_history/news_YYYY-MM-DD.json`：每次面板刷新把新快讯去重合并归档（单日上限 800 条）。
- `day_summary()`：全天多空计数 + 当日利空榜/利好榜 TOP10（按强度排序）。
- AI 上下文升级：提示词含"今日全天累计快讯统计"并注明权重高于最新快讯一节；指纹纳入当日多空分布与新入榜标题。
- 要闻快递标题栏显示累计口径：`实时快讯 · 截至 HH:MM · 今日累计 利好X/利空Y（共Z条）`。
- 指南的板块情绪统计改用全天归档（此前只用单次快照）。

**重启后看板打不开（根因修复）**
- 自启动用 python.exe 会弹黑色控制台，窗口被关 → 服务死。改 `pythonw.exe` 无窗口自启动 + dashboard/fundos_core 无控制台 stdout 守卫 + 重复启动端口冲突优雅退出。
- 桌面创建「FundOS看板.url」快捷方式一键打开（install 自动创建，兼容 OneDrive 桌面）。

**验证**：42/42 单元测试通过（新增指南引擎 6 用例、情绪阈值 1 用例、归档统计）；端到端生成 AI 研判实测引用真实持仓。

---

## 2026-08-29 · 可视化面板（报纸风）+ 自动化补跑

- `dashboard.py` + `dashboard.html`：本地网页看板（127.0.0.1:8899），报纸排版风格（报头/数据速览条/双线分栏）；ECharts 图标库本地化（`data/static/echarts.min.js`，不再依赖 CDN）；「立即刷新」按钮带可见反馈；图表单图容错；红涨绿跌 A 股配色；`--lan` 手机同 WiFi 访问；`--port` 换端口。
- 新闻直链：三大源原文 URL 全部接通，可点击跳转原文。
- 补跑机制：三时点任务改 PowerShell 注册（StartWhenAvailable 错过补跑 + WakeToRun 睡眠唤醒）；`auto_pipeline.py catchup` 登录补跑当天遗漏（用户级 Run 键 `FundOS-Catchup`，因 AtLogOn 计划任务需管理员）；状态记录 `data/state/auto_state.json`，幂等不重复。
- `FundOS-Dashboard` Run 键：看板登录常驻（弹窗点击跳转的前提）。
- 单元测试 22 → 35 项。

---

## 2026-08-28/29 · 路线图四阶段（策略验证 + 信号增强 + XIRR）

- 阶段2：`backtest.py` 规则回测（-15%评估线分批退出/-25%硬止损/止盈 vs 死扛，--aip 定投对照）；`fundos_analytics.py` 扩展净值分位、两两相关性（科技簇均值）、XIRR（基于 transactions.json，二分求解）。
- 阶段3：QDII 估值叠加汇率因子（新浪 fx_susdcnh）；情绪指数（0-100，条数占比+阻尼口径，落盘 `data/sentiment_history/`，进晚报）；主线轮动打分（连续在榜50%+排名改善30%+成交趋势20%，板块历史满2天自动启用）；akshare 装机 1.18.94 并挂为净值历史第三级备源（lsjz → pingzhongdata → akshare）。
- 阶段4：XIRR 并入分析引擎（实测组合 +30.48%/年，资金加权口径）。
- 单元测试扩至 34 项；文档（AGENTS/README/ARCHITECTURE/requirements）同步。

---

## 2026-08-28 · 阶段1：自动化闭环

- `notify.py`：Windows toast（PowerShell WinRT，UTF-16 EncodedCommand 防转义）+ 可选 Server酱微信（`FUNDOS_SCT_KEY` 环境变量启用）+ 按天日志 `data/logs/`。
- `auto_pipeline.py`：morning 晨报(09:00) / afternoon 触发警报(15:30) / evening 晚报(20:30全引擎) 三条流水线；`install/uninstall/status` 计划任务管理；简报生成纯函数可单测。
- 注册 Windows 计划任务三件套并实测（当时 schtasks 版本，后于 08-29 升级为 PowerShell+补跑设置）。

---

## 2026-08-28 · 引擎大升级 v3.0（四层架构，参考 akshare/OpenBB Provider 模式）

**新增三层**
- `fundos_config.py` 统一配置层：9 只持仓（A/C 分开）/ App 摊薄成本口径 cost_nav 校准公式 / 阈值 / 路径——唯一事实来源，替代此前 5 处互相矛盾的复制。
- `fundos_data.py` 统一数据层：重试+指数退避、多源回退链、HTTP 磁盘缓存（TTL）、净值历史全量落盘、板块快照落盘。
- `fundos_analytics.py` 组合分析引擎：最大回撤/年化波动/夏普/相关性/风险贡献/HHI（此前完全缺失）。

**修复 6 个数据级 bug**
1. `fundos_core` 按基金名合并盘后结果（全名 vs 短名永远合不上）→ 改按代码。
2. `daily_check` 持仓表缺 019759（半导体A）→ 9 只对齐快照 + 结果原子写入。
3. `volatility_aug` 用纠错前错误代码（018354/019173/020684/019024）→ 走统一配置。
4. 新闻引擎 12 源仅 1 源存活（cls 405 / searchapi 解析失败 / push2 封禁 / 政府网空）→ v2.1 重写为已验证四源，并修复"搜索结果全标快讯"的维度错标。
5. 东财搜索必须 urllib 直连——requests 会对 URL 百分号编码做规范化导致返回错误维度（已写进代码注释）。
6. QDII（占仓 47%）今日估值恒为 0 → 美股指数代理实 estimation。

**其他**：`fund_engine.py` 三阶段并行编排 + `--offline` + analytics 步骤；`market_scan.py` 主线连续性 + 板块关键词匹配；`tests/test_fundos.py` 22 项；净产值数据沉淀目录体系建立。

---

## 2026-08-21/22（历史，接手前）

- 技能库吞噬融合（buffett-perspective 等，21→22 技能）；`sector-rotation-detector` 目录化；旧脚本归档 `_archive/`；总裁批示_2026-08-22.md。
- 完整留痕：`.codex/skills/fundos/reports/2026-08-21_ingestion.md`。
